<div class="container px-5 px-sm-0">
  <div class="row">

    <% if flash[:notice] %>
   　<p id="notice">
    <%= flash[:notice] %>
    <% end %>

    <div class="col-md-3">
    <%= render 'books/list', book: @book, user: @user %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table tavle-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <%= link_to user_path(@user.id) do %>
              <%= attachment_image_tag @user,:profile_image, size: '50x50', fallback: "no_image.jpg" %><br>
              <%= @user.name %>
              <% end %>
            </td>

            <td><%= link_to @mybook.title,book_path(@mybook.id) %></td>
            <td><%= @mybook.body %></td>
            <% if @user.id == current_user.id %>
            <td><%= link_to "Edit", "/books/#{@mybook.id}/edit" %></td>
            <td><%= link_to "Destroy", "/books/#{@mybook.id}",method: :delete, "data-confirm" => "Are you sure?" %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>

  </div>
</div>